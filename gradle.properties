# ========================================
# Dubbo Plugin Project Configuration
# ========================================

# Project Metadata
group=org.apache.dubbo
# Version synced with Dubbo core version
version=3.3.5-SNAPSHOT

# Build Configuration
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.configureondemand=false

# Kotlin DSL (if needed in future)
kotlin.code.style=official

# Publishing Configuration (uncomment and configure when ready to publish)
# signing.keyId=YOUR_KEY_ID
# signing.password=YOUR_PASSWORD
# signing.secretKeyRingFile=/path/to/secring.gpg
# ossrhUsername=YOUR_USERNAME
# ossrhPassword=YOUR_PASSWORD

